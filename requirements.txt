pandas
numpy
scikit-learn
PyYAML
xgboost
sdv
concrete-ml
# For the FHE venv (.venv-fhe), after `pip install concrete-ml`, swap in the
# GPU-enabled compiler to use config/fhe.yaml's `device: cuda`:
#   pip uninstall concrete-python
#   pip install --extra-index-url https://pypi.zama.ai/gpu concrete-python
seaborn
matplotlib
psutil
