pandas
numpy==1.26.4
scikit-learn
PyYAML
seaborn
matplotlib
psutil

# synthcity calls scipy.integrate.trapz, removed in 1.14
scipy<1.14

synthcity

# Pydantic 2.13 added a breaking check that rejects unannotated class attributes
# on BaseModel subclasses — synthcity's Plugin base class has `protected_cols`
# without a ClassVar annotation, causing all synthesizers to fail at init.
pydantic<2.13

# torch.nn.RMSNorm required by current synthcity; added in 2.4
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.4.0,<2.5.0

# cupy-cuda12x: CUDA 12.5+ ships libnvrtc.so.13 (not .so.12); if cupy fails
# to load at runtime the code falls back to XGBoost CPU mode (see _to_cuda)
xgboost<3.0.0
cupy-cuda12x>=13.0
