-r base.txt
concrete-ml

# For the FHE venv (.venv-fhe), after `pip install -r requirements/azure-concrete-ml.txt`,
# swap in the GPU-enabled compiler to use config/fhe.yaml's `device: cuda`:
#   pip uninstall concrete-python
#   pip install --extra-index-url https://pypi.zama.ai/gpu concrete-python
