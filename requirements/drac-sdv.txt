-r base.txt
sdv

# DRAC has no internet access for pip dependency resolution, so torch must be
# pinned to a wheel tag available in DRAC's offline wheelhouse. ctgan (used by
# sdv) needs torch, so this venv needs the same pin as drac-synthcity.txt.
# Before `pip install -r requirements/drac-sdv.txt`:
#   module load python/3.11
#   module load cuda/12.2
# If the pin below stops being available, find a current tag with:
#   avail_wheels torch
torch==2.4.1+computecanada
torchvision
xgboost

# GPU XGBoost (device='cuda') needs cupy to hand XGBoost a GPU-resident
# array — see Model._to_cuda() in src/models.py. DRAC's wheelhouse builds
# cupy against whichever cuda module is loaded, so leave it unpinned here
# (no -cuda12x suffix like the PyPI azure-*.txt builds). If install fails,
# check available builds with: avail_wheels cupy
cupy